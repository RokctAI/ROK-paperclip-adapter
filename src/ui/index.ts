/**
 * UI module exports — used by Paperclip's dashboard for run viewing
 * and agent configuration forms.
 */

export { parseRokStdoutLine } from "./parse-stdout.js";
export { buildRokConfig } from "./build-config.js";
